<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="keywords" content="Trip In Admin Dashboard" />
    <meta name="description" content="Admin Dashboard for Trip In" />
    <meta name="author" content="Trip In Team" />
    <link rel="shortcut icon" href="{{ asset('images/fevicon.png') }}" type="image/png">

    <title>{% block title %}Trip In - Admin Dashboard - Profil Utilisateur{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Fonts style -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&display=swap" rel="stylesheet">
    <!-- Custom styles -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
    <link href="{{ asset('css/responsive.css') }}" rel="stylesheet" />

    <!-- Inline Styles -->
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f6f9;
            color: #333;
        }
        .sidebar {
            background: #2c3e50;
            color: white;
            padding: 20px;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
            height: 100vh;
        }
        .sidebar .brand {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-align: center;
        }
        .sidebar .nav-link {
            color: #bdc3c7;
            padding: 10px 15px;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .sidebar .nav-link:hover, .sidebar .nav-link.active {
            background: #34495e;
            color: white;
        }
        .content {
            padding: 20px;
        }
        .profile-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
        }
        .btn-primary {
            background: #3498db;
            border: none;
            transition: background 0.3s;
        }
        .btn-primary:hover {
            background: #2980b9;
        }
        .btn-secondary {
            background: #6c757d;
            border: none;
            transition: background 0.3s;
        }
        .btn-secondary:hover {
            background: #5a6268;
        }
    </style>
</head>

<body>
    {% block content %}
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar d-flex flex-column justify-content-between p-3">
                <div>
                    <div class="brand">
                        <a href="{{ path('landing_index') }}" class="text-white text-decoration-none">Trip In Admin</a>
                    </div>
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">
                                <i class="fas fa-tachometer-alt me-2"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_users') }}">
                                <i class="fas fa-users me-2"></i> Users
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">
                                <i class="fas fa-file-alt me-2"></i> Publications
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">
                                <i class="fas fa-list me-2"></i> Destinations
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">
                                <i class="fas fa-cogs me-2"></i> Offers
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">
                                <i class="fas fa-envelope me-2"></i> Contact Requests
                            </a>
                        </li>
                    </ul>
                </div>
                <form action="{{ path('app_logout') }}" method="POST">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                    <button type="submit" class="btn btn-danger mt-2">
                        <i class="fas fa-sign-out-alt me-1"></i>Déconnexion
                    </button>
                </form>
            </div>

            <!-- Main Content -->
            <div class="col-md-10 content">
                <h2 class="mb-4">Profil Utilisateur</h2>

                <div class="card shadow profile-card">
                    <div class="card-body">
                        <h5 class="card-title">Informations Personnelles</h5>
                        <p><strong>Nom :</strong> {{ user.nom|default('N/A') }}</p>
                        <p><strong>Prénom :</strong> {{ user.prenom|default('N/A') }}</p>
                        <p><strong>Email :</strong> {{ user.email|default('N/A') }}</p>
                        <p><strong>Rôle :</strong> {{ user.roles[0]|default('N/A') }}</p>

                        <a href="{{ path('profile_edit') }}" class="btn btn-primary mt-3">
                            <i class="fas fa-edit me-1"></i>Modifier le profil
                        </a>
                        <a href="{{ path('profile_change_password') }}" class="btn btn-secondary mt-3 ms-3">
                            <i class="fas fa-key me-1"></i>Modifier le mot de passe
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.4.1/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    {% endblock %}
</body>
</html>