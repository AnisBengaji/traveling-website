<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des offres</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .title {
            font-size: 24px;
            color: #2c3e50;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .offer-image {
            max-width: 100px;
            height: auto;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1 class="title">Liste des offres</h1>
    </div>

    <table>
        <thead>
            <tr>
                <th>Image</th>
                <th>Titre</th>
                <th>Destination</th>
                <th>Prix</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            {% for offer in offers %}
                <tr>
                    <td>
                        {% if offer.image %}
                            <img src="{{ absolute_url(asset('uploads/offers/' ~ offer.image)) }}" alt="{{ offer.titre }}" class="offer-image">
                        {% endif %}
                    </td>
                    <td>{{ offer.titre }}</td>
                    <td>{{ offer.destination }}</td>
                    <td>{{ offer.prix }} €</td>
                    <td>{{ offer.description }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html> 