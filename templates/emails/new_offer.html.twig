<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouvelle offre disponible</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .offer-details {
            background-color: #fff;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Nouvelle offre disponible !</h1>
    </div>

    <div class="offer-details">
        <h2>{{ offre.titre }}</h2>
        <p>{{ offre.description }}</p>
        <p><strong>Destination:</strong> {{ offre.destination }}</p>
        <p><strong>Prix:</strong> {{ offre.prix }} €</p>
        
        {% if offre.image %}
            <img src="{{ absolute_url(asset('uploads/images/' ~ offre.image)) }}" alt="{{ offre.titre }}" style="max-width: 100%; margin: 20px 0;">
        {% endif %}

        <a href="{{ absolute_url(path('landing_service_show', {'id': offre.id})) }}" class="button">
            Voir les détails de l'offre
        </a>
    </div>

    <div class="footer">
        <p>Vous recevez cet email car vous êtes inscrit à notre newsletter.</p>
        <p>Pour vous désinscrire, cliquez <a href="{{ absolute_url(path('unsubscribe', {'email': user.email})) }}">ici</a>.</p>
    </div>
</body>
</html> 